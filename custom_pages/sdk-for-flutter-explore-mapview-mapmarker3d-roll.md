---
title: "roll property"
slug: "sdk-for-flutter-explore-mapview-mapmarker3d-roll"
---

<HTMLBlock>{
`
<div class="sdk-for-flutter">
<!-- roll.html -->


<div>
<h1>roll property</h1></div>
<section id="getter">

double
roll


<p>The roll angle of the 3D model in degrees.
The roll axis is parallel to the ground, passes through the 3D marker's
location and is aligned initially with the true North. However, when the bearing changes,
it rotates around the bearing axis with the 3D marker.
Positive/negative values cause a clockwise/counterclockwise rotation when viewing along the axis
in the direction of the true North. The Y-axis of the model is aligned with the roll axis.
Gets the roll of the 3D model in degrees.</p>


<h2>Implementation</h2>
<pre class="language-dart"><code class="language-dart">double get roll;</code></pre>

</section>
<section id="setter">

void
roll=(double value)


<p>The roll angle of the 3D model in degrees.
The roll axis is parallel to the ground, passes through the 3D marker's
location and is aligned initially with the true North. However, when the bearing changes,
it rotates around the bearing axis with the 3D marker.
Positive/negative values cause a clockwise/counterclockwise rotation when viewing along the axis
in the direction of the true North. The Y-axis of the model is aligned with the roll axis.
Sets the roll of the 3D model in degrees.</p>


<h2>Implementation</h2>
<pre class="language-dart"><code class="language-dart">set roll(double value);</code></pre>

</section>
 



</div>
`
}</HTMLBlock>
