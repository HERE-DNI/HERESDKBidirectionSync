---
title: "isRouteVisible property"
slug: "sdk-for-flutter-navigate-navigation-visualnavigator-isroutevisible"
---

<HTMLBlock>{
`
<div class="sdk-for-flutter">
<!-- isRouteVisible.html -->


<div>
<h1>isRouteVisible property</h1></div>
<section id="getter">

bool
isRouteVisible


<p><code>Route</code> visibility which defines whether to perform route rendering during visual navigation.
When enabled, the set <code>Route</code> will be rendered as a <code>MapPolyline</code> together with <code>MapArrow</code> items that
indicate the next turns. By default, it is enabled.
When disabled, <code>MapArrow</code> items are still rendered. To hide arrows, use <code>VisualNavigatorColors</code> with
transparent color.
Gets the current state of route rendering during visual navigation.</p>


<h2>Implementation</h2>
<pre class="language-dart"><code class="language-dart">bool get isRouteVisible;</code></pre>

</section>
<section id="setter">

void
isRouteVisible=(bool value)


<p><code>Route</code> visibility which defines whether to perform route rendering during visual navigation.
When enabled, the set <code>Route</code> will be rendered as a <code>MapPolyline</code> together with <code>MapArrow</code> items that
indicate the next turns. By default, it is enabled.
When disabled, <code>MapArrow</code> items are still rendered. To hide arrows, use <code>VisualNavigatorColors</code> with
transparent color.
Sets whether to perform route rendering during visual navigation.</p>


<h2>Implementation</h2>
<pre class="language-dart"><code class="language-dart">set isRouteVisible(bool value);</code></pre>

</section>
 



</div>
`
}</HTMLBlock>
